---

---

<!-- Button Container -->
<style>

    /* Key Change 1: Smoother Loop
       We move the background position horizontally.
       Using '0% 50%' to '100% 50%' ensures it slides purely sideways.
    */
    @keyframes border-flow {
        0% {
            background-position: 0% 50%;
        }
        100% {
            background-position: 100% 50%;
        }
    }

    /* Key Change 2: Custom Gradient & Speed
       - We define a 'linear-gradient' with hard stops that repeats the first color (#3B82F6) at the end.
       - background-size: 300%: Makes the gradient long, so movement is visible.
       - animation: 2.5s linear infinite: 'Linear' removes the pause at the start/end.
    */
    .animate-border-flow {
        background: linear-gradient(
                90deg,
                #3B82F6,
                #06b6d4,
                #F59E0B,
                #f97316,
                #ec4899,
                #3B82F6
        );
        background-size: 300% 100%;
        animation: border-flow 2.5s linear infinite;
    }
</style>

<div class="relative group">

    <!--
      1. The Animated Glow Layer (Shadow)
         - Uses .animate-border-flow for movement.
         - Retains blur-2xl for the "pop".
    -->
    <div class="animate-border-flow absolute -inset-1 rounded-full blur-2xl opacity-0 group-hover:opacity-100 transition duration-500 group-hover:duration-200"></div>

    <!--
      2. The Animated Border Layer
         - Uses .animate-border-flow for movement.
         - This layer sits right behind the white button to create the stroke.
    -->
    <div class="animate-border-flow absolute -inset-[2px] rounded-full opacity-100 blur-[0.5px]"></div>

    <!-- 3. The Button Content -->
    <button class="relative px-8 py-3 bg-white rounded-full flex items-center gap-4 leading-none transition-all duration-300 ease-out group-hover:-translate-y-1 group-hover:scale-[1.02] active:scale-95 active:translate-y-0">

        <!-- Text -->
        <span class="text-slate-800 font-semibold text-lg tracking-tight">
                Let's chat!
            </span>

        <!-- Icon: Chat Bubble -->
        <div class="w-7 h-7 relative">
            <svg viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg" class="w-full h-full drop-shadow-sm">
                <defs>
                    <linearGradient id="chatGradient" x1="2" y1="2" x2="22" y2="22" gradientUnits="userSpaceOnUse">
                        <stop stop-color="#3B82F6" />
                        <stop offset="1" stop-color="#F59E0B" />
                    </linearGradient>
                </defs>
                <path d="M12 22C17.5228 22 22 17.5228 22 12C22 6.47715 17.5228 2 12 2C6.47715 2 2 6.47715 2 12C2 13.5997 2.37562 15.1116 3.04346 16.4525C3.22094 16.8088 3.28001 17.2161 3.17712 17.6006L2.58151 19.8267C2.32295 20.793 3.20701 21.677 4.17335 21.4185L6.39939 20.8229C6.78393 20.72 7.19121 20.7791 7.54753 20.9565C8.88837 21.6244 10.4003 22 12 22Z" fill="url(#chatGradient)"/>
            </svg>
        </div>
    </button>
</div>